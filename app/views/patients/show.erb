<h1> Patient: <%= @patient.name %> </h1>
<ul> <% @appointments.each do |app| %>
  <li> Doctor: <%= link_to app.doctor.name, doctor_path(app.doctor) %> </li>
  <li> Time: <%= app.appointment_datetime.strftime("%B %e, %Y at %k:%M.")%> </li>
<% end %>
</ul>
