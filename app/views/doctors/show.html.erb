<h1><%=@doctor.name%></h1>

<h2>Schedule:</h2>
<ul>
  <% @doctor.appointments.each do |appt| %>

    <li><%=appt.human_readable_date%> with <%= link_to appt.patient.name, patient_path(appt.patient)%></li>

  <%end%>
</ul>
