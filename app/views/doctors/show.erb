<h1> <%= @doctor.name %> </h1>

<ul>
<% @doctor.appointments.each do |appt| %>
<li> <%= link_to "#{appt.patient.name} - #{appt.appointment_datetime.strftime('%B %d, %Y at %R')}", appt.patient %> </li>
<% end %>
</ul>
